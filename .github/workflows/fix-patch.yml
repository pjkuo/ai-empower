name: fix-patch
on: push
permissions:
  contents: write
jobs:
  patch:
    if: ${{ contains(github.event.head_commit.message, 'fix-run') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/fix_patch.py
      - run: |
          git config user.name "pjkuo"
          git config user.email "kuo0615@gmail.com"
          git rm -q .github/workflows/fix-patch.yml .github/fix_patch.py
          git add -A
          git commit -m "assess.html 錯字修正（搝→搶、措→揪、竴→攤）" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_0132fkQFFpgYs7gD13EjefU8"
          git push
